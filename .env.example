# ─── OAuth (recommended) ───────────────────────────────────────
# No config needed! Just open the web UI and login with your Mastodon account.
# The app will register itself and handle authentication automatically.

# The URL where this app is reachable (used for OAuth redirect)
APP_URL=http://localhost:8080

# ─── Manual token (optional, for headless/automated setups) ────
# If set, these are used as fallback when no OAuth login has been done.
# MASTODON_INSTANCE=https://mastodon.social
# MASTODON_ACCESS_TOKEN=your_access_token_here

# ─── General settings ─────────────────────────────────────────
# Poll interval in minutes (default: 5)
POLL_INTERVAL=5

# Database path (inside the container)
DB_PATH=/app/data/tootkeeper.db
